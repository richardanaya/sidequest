/**
 * No game content lives here.
 *
 * Each game supplies items from its content folder:
 *   {contentRoot}/items.json
 *   {contentRoot}/items/<id>.png   (green-screen plate or keyed PNG)
 *
 * See content.json → defaults.items
 */
export const ITEMS = {};
